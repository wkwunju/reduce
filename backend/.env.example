# ============================================================================
# XTrack Backend Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# Twitter API Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Get your API key from: https://twitterapi.io/dashboard
# 1. Sign up at https://twitterapi.io
# 2. Go to Dashboard
# 3. Copy your API key
TWITTER_API_KEY=your_twitter_api_key_here
TWITTER_API_BASE_URL=https://api.twitterapi.io

# ----------------------------------------------------------------------------
# LLM Configuration (REQUIRED - choose one)
# ----------------------------------------------------------------------------
# Option 1: Gemini (Recommended)
# Get your API key from: https://makersuite.google.com/app/apikey
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Click "Create API Key"
# 3. Copy the key
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Option 2: OpenAI (Alternative)
# Get your API key from: https://platform.openai.com/api-keys
# 1. Go to https://platform.openai.com/api-keys
# 2. Click "Create new secret key"
# 3. Copy the key
# OPENAI_API_KEY=your_openai_api_key_here

# ----------------------------------------------------------------------------
# Gmail API Configuration (OPTIONAL - for sending summaries via email)
# ----------------------------------------------------------------------------
# This app uses Gmail API with OAuth 2.0 (more secure than SMTP)
#
# Setup Instructions:
# 1. Go to Google Cloud Console: https://console.cloud.google.com
# 2. Create a new project (or select existing one)
# 3. Enable Gmail API:
#    - Go to "APIs & Services" > "Library"
#    - Search for "Gmail API"
#    - Click "Enable"
# 4. Create OAuth 2.0 Credentials:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "OAuth client ID"
#    - Application type: "Desktop app"
#    - Name it (e.g., "XTrack Email")
#    - Click "Create"
# 5. Download the credentials:
#    - Click the download icon next to your OAuth client
#    - Save as "credentials.json" in the backend directory
# 6. First run:
#    - When you start the server, it will open a browser for OAuth authorization
#    - Sign in with your Gmail account
#    - Grant permissions
#    - A "token.pickle" file will be created automatically
#
# Configuration (optional, uses defaults if not set):
GMAIL_CREDENTIALS_FILE=credentials.json
GMAIL_TOKEN_FILE=token.pickle
FROM_EMAIL=your_email@gmail.com

# Note: After first authorization, token.pickle will be used automatically
# You only need to authorize once, unless you revoke access or delete token.pickle

# ----------------------------------------------------------------------------
# Telegram Configuration (OPTIONAL - for notifications)
# ----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_WEBHOOK_DEV=https://<your-ngrok-domain>/api/notifications/telegram/webhook
TELEGRAM_WEBHOOK_RAILWAY=https://<your-railway-domain>/api/notifications/telegram/webhook

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000

# ----------------------------------------------------------------------------
# Notes:
# ----------------------------------------------------------------------------
# - Never commit .env, credentials.json, or token.pickle to git
# - They are in .gitignore for security
# - Restart the server after changing these values
# - Email configuration is optional - the app works without it
# - If email is not configured, summaries will only be shown in the UI
# - Gmail API is more secure than SMTP and doesn't require App Passwords
# ============================================================================
